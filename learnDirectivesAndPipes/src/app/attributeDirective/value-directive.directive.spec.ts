import { ValueDirectiveDirective } from './value-directive.directive';

describe('ValueDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ValueDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
